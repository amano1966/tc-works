import { p as proxyCustomElement, H, d as createEvent, h, F as Fragment, c as Host } from './p-85681586.js';
import { d as defineCustomElement$2 } from './p-56367692.js';
import { d as defineCustomElement$1 } from './p-03a9687b.js';

const modusTableRowActionsCss = "modus-table-row-actions{align-items:center;display:flex;gap:8px;justify-content:center}";
const ModusTableRowActionsStyle0 = modusTableRowActionsCss;

const ModusTableRowActions = /*@__PURE__*/ proxyCustomElement(class ModusTableRowActions extends H {
    constructor() {
        super();
        this.__registerHost();
        this.overflowRowActions = createEvent(this, "overflowRowActions", 7);
        this.row = undefined;
        this.context = undefined;
    }
    handleMoreButtonClick(e, menu) {
        const { left, top, height } = this.overflowButtonRef.getBoundingClientRect();
        this.overflowRowActions.emit({
            componentId: this.context.componentId,
            actions: menu,
            position: { x: left, y: top + height },
            row: this.row,
            onClose: () => this.overflowButtonRef.focusButton(),
        });
        e.preventDefault();
    }
    handleMoreButtonKeydown(e, menu) {
        if (e.key.toLowerCase() === 'enter') {
            this.handleMoreButtonClick(e, menu);
            e.preventDefault();
        }
    }
    handleActionButtonClick(e, actionId) {
        const { rowActionClick } = this.context;
        rowActionClick.emit({ actionId, row: this.row.original });
        e.preventDefault();
    }
    handleActionButtonKeydown(e, actionId) {
        if (e.key.toLowerCase() === 'enter') {
            this.handleActionButtonClick(e, actionId);
            e.preventDefault();
        }
    }
    render() {
        const { rowActions } = this.context;
        let actionButtons;
        let overflowMenu;
        if (rowActions) {
            actionButtons = rowActions.filter((action) => !action.isOverflow);
            overflowMenu = rowActions.filter((action) => action.isOverflow);
        }
        return (h(Host, { key: '65214098300bd314fc207751b352b24f36753203' }, actionButtons === null || actionButtons === void 0 ? void 0 :
            actionButtons.map(({ label, icon, id, tooltipText, buttonStyle, color, isDisabled = () => false }) => {
                const disabled = isDisabled(this.row.original);
                return (h("div", null, h("modus-tooltip", { text: tooltipText, position: "top" }, h("modus-button", { class: "row-actions", "button-style": buttonStyle ? buttonStyle : 'borderless', color: color ? color : 'secondary', "icon-only": icon, size: "small", ariaLabel: label, disabled: disabled, onKeyDown: (e) => this.handleActionButtonKeydown(e, id), onClick: (e) => (!disabled ? this.handleActionButtonClick(e, id) : e.preventDefault()) }, label)), ' '));
            }), (overflowMenu === null || overflowMenu === void 0 ? void 0 : overflowMenu.length) > 0 && (h(Fragment, null, h("modus-button", { ref: (el) => (this.overflowButtonRef = el), class: "row-actions-menu-button", "button-style": "borderless", color: "secondary", "icon-only": "more_vertical", ariaLabel: "overflow button", size: "small", onKeyDown: (e) => this.handleMoreButtonKeydown(e, overflowMenu), onClick: (e) => this.handleMoreButtonClick(e, overflowMenu) })))));
    }
    static get style() { return ModusTableRowActionsStyle0; }
}, [0, "modus-table-row-actions", {
        "row": [16],
        "context": [16]
    }]);
function defineCustomElement() {
    if (typeof customElements === "undefined") {
        return;
    }
    const components = ["modus-table-row-actions", "modus-button", "modus-tooltip"];
    components.forEach(tagName => { switch (tagName) {
        case "modus-table-row-actions":
            if (!customElements.get(tagName)) {
                customElements.define(tagName, ModusTableRowActions);
            }
            break;
        case "modus-button":
            if (!customElements.get(tagName)) {
                defineCustomElement$2();
            }
            break;
        case "modus-tooltip":
            if (!customElements.get(tagName)) {
                defineCustomElement$1();
            }
            break;
    } });
}

export { ModusTableRowActions as M, defineCustomElement as d };
