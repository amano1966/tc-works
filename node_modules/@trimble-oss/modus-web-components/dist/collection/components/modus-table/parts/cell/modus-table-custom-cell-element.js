import { h, // eslint-disable-line @typescript-eslint/no-unused-vars
 } from "@stencil/core";
export const ModusTableCustomCellElement = ({ customElement }) => {
    return h("div", { class: "cell-custom" }, " ", h("flex-renderer", { content: customElement.getValue().toString() }));
};
