import { h, // eslint-disable-line @typescript-eslint/no-unused-vars
 } from "@stencil/core";
export const ModusTableHeaderCheckbox = ({ context }) => {
    const { tableInstance: { getIsAllRowsSelected, getIsSomeRowsSelected, getToggleAllRowsSelectedHandler }, rowSelectionOptions, density, } = context;
    const densityWidths = new Map([
        ['compact', '38px'],
        ['comfortable', '46px'],
        ['relaxed', '46px'],
    ]);
    let checkboxSize = 'medium';
    if (density === 'compact') {
        checkboxSize = 'small';
    }
    const width = densityWidths.get(density) || '46px';
    return (h("th", { class: 'row-checkbox sticky-left ' + checkboxSize, style: { width: width } }, (rowSelectionOptions === null || rowSelectionOptions === void 0 ? void 0 : rowSelectionOptions.multiple) && (h("modus-checkbox", { ariaLabel: "Select all rows", checked: getIsAllRowsSelected(), indeterminate: getIsSomeRowsSelected(), size: checkboxSize, onCheckboxClick: getToggleAllRowsSelectedHandler() }))));
};
