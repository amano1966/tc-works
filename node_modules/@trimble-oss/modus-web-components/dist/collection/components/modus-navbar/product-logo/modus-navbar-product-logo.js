// eslint-disable-next-line
import { h } from "@stencil/core";
export const ModusNavbarProductLogo = ({ logos, onClick }) => {
    var _a, _b;
    const { primary, secondary, clickable } = logos || {};
    const handleKeyDown = (event) => {
        if (event.key !== 'Enter' && event.key !== ' ') {
            return;
        }
        if (onClick) {
            onClick(event);
        }
    };
    const primaryLogoClasses = `product-logo-primary ${clickable ? 'logo-clickable' : ''} ${secondary !== undefined ? 'secondary-logo-exists' : ''}`;
    const secondaryLogoClasses = `product-logo-secondary ${clickable ? 'logo-clickable' : ''} ${primary !== undefined ? 'primary-logo-exists' : ''}`;
    return (h("div", { "aria-label": "Logo", onClick: onClick, onKeyDown: handleKeyDown, tabindex: "0", role: "button", class: "product-logo" }, primary && (h("img", { class: primaryLogoClasses, height: (_a = primary.height) !== null && _a !== void 0 ? _a : '24', src: primary.url, alt: primary.alt ? primary.alt : null, "data-test-id": "primary-logo" })), secondary && (h("img", { class: secondaryLogoClasses, height: (_b = secondary.height) !== null && _b !== void 0 ? _b : '24', src: secondary.url, alt: secondary.alt ? secondary.alt : null, "data-test-id": "secondary-logo" }))));
};
